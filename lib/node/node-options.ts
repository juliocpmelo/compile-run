import {Options} from '../types/interfaces/options'

export interface Node_Options extends Options{

    /**
     * Path to the execution Command for node-js
     * 
     */
    executionPath?: string;
}